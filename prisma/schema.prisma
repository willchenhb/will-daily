generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model DiaryEntry {
  id        Int      @id @default(autoincrement())
  date      String   @unique // YYYY-MM-DD
  content   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model WeeklyPlan {
  id        Int        @id @default(autoincrement())
  weekStart String     @unique // YYYY-MM-DD (Monday)
  title     String?
  content   String?    // Weekly reflection HTML
  todos     TodoItem[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
}

model TodoItem {
  id           Int        @id @default(autoincrement())
  weeklyPlanId Int
  weeklyPlan   WeeklyPlan @relation(fields: [weeklyPlanId], references: [id], onDelete: Cascade)
  text         String
  completed    Boolean    @default(false)
  note         String?    // Feedback/notes on completion
  order        Int
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@index([weeklyPlanId])
}

model Note {
  id        Int      @id @default(autoincrement())
  title     String
  content   String
  category  String?  // Lowercase, trimmed
  summary   String?  // AI-generated
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model CuratedArticle {
  id        Int      @id @default(autoincrement())
  url       String   @unique
  title     String
  image     String?  // Cover image URL
  content   String?  // Extracted article text
  summary   String?  // Kimi-generated summary
  tags      String?  // Comma-separated auto-generated tags
  source    String?  // e.g. "wechat"
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Setting {
  id        Int      @id @default(autoincrement())
  key       String   @unique
  value     String
  updatedAt DateTime @updatedAt
}
